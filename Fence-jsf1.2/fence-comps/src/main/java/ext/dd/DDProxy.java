package ext.dd;

import javax.faces.context.FacesContext;

/**
 *Note:This java code is auto generated by abner,do not edit it. A DragDrop
 * implementation that inserts an empty, bordered div into the document that
 * follows the cursor during drag operations. At the time of the click, the
 * frame div is resized to the dimensions of the linked html element, and moved
 * to the exact location of the linked element. References to the "frame"
 * element refer to the single proxy element that was created to be dragged in
 * place of all DDProxy elements on the page.
 */
public class DDProxy extends DD {
	public static final String COMPONENT_TYPE = "Ext.dd.DDProxy";

	/**
	 * <p>
	 * Create a new {@link DDProxy} instance with default property values.
	 * </p>
	 */
	public DDProxy() {
		super();
		setRendererType(COMPONENT_TYPE);
	}

	public String getFamily() {
		return (COMPONENT_TYPE);
	}

	private Object[] _values;

	public Object saveState(FacesContext _context) {
		if (_values == null) {
			_values = new Object[1];
		}
		_values[0] = super.saveState(_context);

		return _values;
	}

	public void restoreState(FacesContext _context, Object _state) {
		_values = (Object[]) _state;
		super.restoreState(_context, _values[0]);

	}
}