package ext.dd;

import javax.faces.context.FacesContext;

/**
 *Note:This java code is auto generated by abner,do not edit it. A DragDrop
 * implementation that does not move, but can be a drop target. You would get
 * the same result by simply omitting implementation for the event callbacks,
 * but this way we reduce the processing cost of the event listener and the
 * callbacks.
 */
public class DDTarget extends DragDrop {
	public static final String COMPONENT_TYPE = "Ext.dd.DDTarget";

	/**
	 * <p>
	 * Create a new {@link DDTarget} instance with default property values.
	 * </p>
	 */
	public DDTarget() {
		super();
		setRendererType(COMPONENT_TYPE);
	}

	public String getFamily() {
		return (COMPONENT_TYPE);
	}

	private Object[] _values;

	public Object saveState(FacesContext _context) {
		if (_values == null) {
			_values = new Object[1];
		}
		_values[0] = super.saveState(_context);

		return _values;
	}

	public void restoreState(FacesContext _context, Object _state) {
		_values = (Object[]) _state;
		super.restoreState(_context, _values[0]);

	}
}